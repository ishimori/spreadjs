# DD-052-5: 見出しクリックとホバーの通知（RC5・RC6）

| 作成日 | 更新日 | ステータス | 補足 |
|--------|--------|-----------|------|
| 2026-09-12 | 2026-09-12 | 検討中 | 親=DD-052（consumer 駆動: ReadyCrew RC5・RC6） |

> アプローチ: 標準＋TDD（純関数の単体）＋E2E駆動（playground ポインタ操作）
> リスク: なし（認可・DBスキーマ・外部I/F・機密情報に触れない）

```text
Risk Class: B（承認済みバックログ範囲内の新規イベント追加のみ。既存のポインタ処理〔選択・リサイズ・リンク列カーソル切替〕の分岐に新設イベント発火を追加するだけで、既存の分岐結果は変えない）
Risk Triggers: なし（新規 GridEvent 追加のみ。購読しない consumer への挙動変化なし）
Human Spec Gate: skipped（RC5・RC6 の形は親 DD-052 論点6 で確定済み。本DDは実装のみ）
Codex: high
Manual Gate: なし（E2E で判定できる）
External Review: なし
Evidence Level: standard
```

## 目的

ReadyCrew RC5（列見出しのクリック通知）・RC6（セルのホバー通知）を実装する。並べ替え自体・ツールチップ描画自体は consumer 側の責務のまま、SDK は通知と最小限の表示オプションのみを提供する。

## 背景・課題

- RC5 現状: `pointerdown` ハンドラ（`mount-controller.ts:1258-1329`、該当1313-1316）は `hit.area`（`'cell' | 'row-header' | 'column-header' | 'corner'`）を区別せず、`cell` 以外は一律 `editor.pointerdownCell(null)`（選択解除）のみ。列見出しクリックを検知する分岐が無い
- RC6 現状: `pointermove` ハンドラ（1134-1219行目、該当1135-1210）はリサイズドラッグ・選択ドラッグ・カーソル切替のみを扱い、ホバー通知は無い。`GridEvent`（`index.ts`）は `connection`/`pending`/`rejected`/`divergence`/`error`/`layout`/`cell-commit`/`row-structure-change`/`link-open`/`remote-change`/`presence` の10種のみ
- 親 DD-052 決定（論点6）: RC4 (a) は DD-052-4。**RC5 提示のとおり**＝`header-click{columnId}` イベント＋並べ替え中を示す表示オプション（並べ替え自体は consumer）。**RC6 (a)**＝`cell-hover{rowId,columnId,rect}`（出入りで発火・間引きあり。consumer が Markdown/HTML で描ける）

## 検討内容

- RC5 の表示オプション（並べ替え中インジケータ）の具体形（列単位の矢印表示か、テキストか）は Phase 1 で既存のヘッダー描画コード（`packages/render/`）を実読して決める
- RC6 の間引き方式（rAF・時間ベースのdebounce等）は Phase 1 で既存の `pointermove` 内の他の処理（リサイズ）と整合させて決める

## 決定事項

- `GridEvent` に `{ type: 'header-click'; columnId: string }` を追加。`pointerdown` が `hit.area === 'column-header'` を検知したら発火する（既存の選択解除等の挙動は変えない）
- `GridCommonMountOptions` に列単位の並べ替えインジケータ表示オプション（例: `columnSortIndicator?: { columnId: string; direction: 'asc' | 'desc' } | null`。具体名・形は Phase 1 で確定し本節へ反映）を追加。SDK は表示するだけで並べ替え自体は行わない
- `GridEvent` に `{ type: 'cell-hover'; rowId: string; columnId: string; rect: { x: number; y: number; width: number; height: number } } | { type: 'cell-hover'; rowId: null; columnId: null }`（セルから離れたら null 系で通知）を追加。`pointermove` で対象セルが変わった時だけ発火し、間引きを入れる

## 受け入れ基準

| # | 基準（操作 → 期待結果） | 検証方法 |
|---|------------------------|---------|
| 1 | 列見出しをクリック → `header-click{columnId}` が発火する。セルクリックの既存挙動（選択・編集開始）は変わらない | unit＋E2E |
| 2 | 並べ替えインジケータ表示オプションを指定 → 該当列見出しに矢印等の表示が出る。オプション未指定では何も出ない（既定は現状維持） | E2E（スクリーンショット） |
| 3 | セル上でポインタを動かす → 対象セルが変わるたびに `cell-hover{rowId,columnId,rect}` が発火し、頻発しない（間引きされる） | unit＋E2E |
| 4 | グリッド外・見出し領域へポインタが出る → `cell-hover` が「離脱」を示す形で1回発火する | unit |
| 5 | いずれのイベントも購読しない consumer の既存挙動・パフォーマンスに変化がない | 全回帰＋既存 perf 予算内 |
| 6 | 公開 .d.ts snapshot の差分が追加のみ。CHANGELOG（Added）・quick-start・features.json 更新 | contract test＋目視 |
| 7 | 既存 unit・invariants・E2E が無修正で green | 全回帰 |

## タスク一覧

### Phase 1: 実読・詳細化
- [ ] 📐 実読: `mount-controller.ts`（pointerdown/pointermove ハンドラ全体）・`packages/render/` のヘッダー描画・`viewport.ts`（`hit.area`）を実読し、並べ替えインジケータの描画方式・`cell-hover` の間引き方式を確定
- [ ] 🔬 機械検証: `bash scripts/doc-check.sh` → OK

### Phase 2: 実装（Red → Green）
- [ ] **Red**: 列見出しクリックで `header-click` が発火する単体ケース、ポインタ移動で `cell-hover` が発火・間引きされる単体ケースを追加 → 失敗を確認
- [ ] **Green**: `pointerdown`/`pointermove` ハンドラへ分岐を追加し実装
- [ ] 🔬 機械検証: `npx vitest run packages/grid` → green

### Phase 3: 統合・ドキュメント
- [ ] playground E2E に見出しクリック・ホバーのケースを追加。CHANGELOG（Added）・quick-start・features.json 更新
- [ ] 🔬 機械検証: `npx playwright test` → green／contract test（.d.ts snapshot 差分=追加のみ）／features smoke／perf 予算回帰なし

### 完了前チェック
- [ ] 受け入れ基準を1項目ずつ照合（未達成があれば理由をログへ）
- [ ] 😈 セルフレビュー1巡（重点: `cell-hover` の発火頻度〔フレーム予算契約への抵触有無〕・リサイズ/選択ドラッグ中に誤発火しないか）
- [ ] 🔬 全回帰1回（`npm test`／`npm run typecheck`／`npm run typecheck:consumer-strict`／`npm run lint`／`npm run test:e2e`）→ 全パス

## ログ

### 2026-09-12
- DD作成。親 DD-052 の子DD分割（決定事項）に基づき起票
