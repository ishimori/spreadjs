# DD-026-2: 認証フック（U2）

| 作成日 | 更新日 | ステータス | 補足 |
|--------|--------|-----------|------|
| 2026-09-03 | 2026-09-03 | 完了 | U2 成立: `serve({ authenticate })`（null=401・throw=500・identity で actorId/presence を上書き）。auth 5 件 green・Codex xhigh P2×2 反映（診断から hook の message を除去／認証待ち socket を stop で破棄） |

```text
Risk Class: A
Risk Triggers: consumer の認証（JWT Cookie）を信頼境界に組み込む／envelope の actorId・presence の userId/displayName をサーバーが上書き（protocol 意味論の変更）／Stable 前の公開 API（ServeOptions）を拡張
Human Spec Gate: 親 DD-026 Phase 1 で代行確定（ユーザー指示「迷ったら推奨案」・2026-09-03）
Codex: high（親でまとめて 1 回・xhigh で実施）
Manual Gate: なし（実機の JWT なし接続拒否は親 Phase 5 の consumer Manual Gate）
External Review: なし
Evidence Level: standard
```

> アプローチ: TDD（拒否・上書きの正解が明確。シナリオ: contract.md §5 A1〜A4）
> リスク: あり（認証 — Phase 1）

## 目的

WebSocket upgrade 時に consumer の `authenticate(request)` で接続を検証し、受理後の操作・presence の身元をサーバーが確定する（クライアント申告を信用しない）。

## 決定事項

contract.md §1 ③・§4 5 を採用。追加の実装者判断:

- hook には Node の `IncomingMessage` を渡さず `{ url, headers }` の最小形（Cookie は `headers.cookie`）。
- 拒否は raw socket へ `HTTP/1.1 401` を書いて destroy（hook の throw は 500）。await 中に socket が閉じたら upgrade しない。
- `authenticate` 未指定時は従来挙動（申告値）。診断 `auth-rejected`/`auth-error` は `onDiagnostic` にのみ出す（値は載せない）。

## 受け入れ基準

| # | 基準 | 検証方法 |
|---|------|---------|
| 1 | null 返却の upgrade は 401 で拒否され接続数 0・診断 `auth-rejected` | `serve.auth.test.ts` A1 |
| 2 | 受理 identity の `actorId` が envelope（oplog・配信）に入り、申告 `actorId` は無視。presence displayName も上書き | 同 A2 |
| 3 | hook の throw は 500 で拒否し、サーバーは稼働継続（他接続に影響なし） | 同 A3 |
| 4 | 未指定なら従来どおり申告値（回帰）。Cookie ヘッダが hook に届く | 同 A4 |

## タスク一覧

### Phase 1: Red→Green
- [x] `packages/server-hono/src/server.ts`: upgrade ハンドラに `authenticate` を挿入（401/500・await 中 close ガード）。RoomBridge に identity を保持し `submitOperation`/`presence` を書き換え
- [x] `packages/server-hono/src/index.ts`: `ServeOptions.authenticate`・公開型 re-export・診断コード
- [x] `packages/server-hono/src/serve.auth.test.ts`: A1〜A4
- [x] 🔬 機械検証: `npx vitest run packages/server-hono` → green／`npm run typecheck`・`npm run lint` → green

### 完了前チェック
- [x] 受け入れ基準 1〜4 を照合（下記ログ）
- [x] 😈 セルフレビュー（await 中の socket close／再接続時も上書きされるか／identity と clientId の関係）

## ログ

### 2026-09-03
- 起票（親 DD-026 Phase 1・論点③ 推奨案で確定）
- 実装完了。AC1〜4 green（`serve.auth.test.ts` 4 件＝A1〜A4）。`clientId` は申告維持（再接続同一性）＝別ユーザーの clientId 乗っ取りは trusted internal の境界として quick-start に明記
- セルフレビュー所見: ①await 中に socket が閉じても `handleUpgrade` は readable/writable 検査で destroy するだけ（受理しない）。認証待ちの間は raw socket の error listener をこの層で持ち、uncaught 化を防ぐ ②再接続時も upgrade ごとに authenticate が走るため identity は毎回確定する（古い identity の持ち越しなし）③reconcile（DD-015）は operationId/clientSequence（clientId 単位）で判定し actorId 非依存＝上書きは整合に影響しない ④ブラウザー WebSocket は 401 を判別できない（close 1006）ため grid は `connect-failed` を通知する＝consumer は自 API で認証状態を先に確認する運用（境界・quick-start に明記済み）
- Codex xhigh（親でまとめて 1 回）本 DD 該当 2 件を**全て採用**: [P2] hook の throw を `errorMessage(error)` のまま診断へ流すと Cookie/トークン/DB 資格情報が載り得る → 種別（`Error.name`）のみに変更・`onDiagnostic` 未指定時は console.error で種別のみ（A3 で「message を含まない」を固定）／[P2] `authenticate` が settle しない間の raw socket は wss 管理外で `stop()` が `server.close` で待ち続ける → 認証待ち socket を追跡し close で destroy（A5 で「stop が 3 秒以内に解決」を固定）
- 完了（AC1〜4 充足）。実機（JWT なし接続拒否・松下）は親 DD の既知の未保証境界へ
