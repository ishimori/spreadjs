# DD-052-3: 編集の制限（RC3: セル単位読み取り専用・RC14: 行操作の無効化）

| 作成日 | 更新日 | ステータス | 補足 |
|--------|--------|-----------|------|
| 2026-09-12 | 2026-09-13 | 完了 | 親=DD-052（consumer 駆動: ReadyCrew RC3・RC14）。実装・全検証green・Codexレビュー指摘2件（P1・P2）を修正済み。0.1.0-alpha.8で配布済み |

> アプローチ: 標準＋TDD（`readonly-policy.ts`・`row-operations.ts` 純関数の単体）
> リスク: なし（認可・DBスキーマ・外部I/F・機密情報に触れない）

```text
Risk Class: B（承認済みバックログ範囲内の Experimental オプション追加。列・行単位の既存 readonly-policy と同型の拡張・デフォルト値は現状維持）
Risk Triggers: なし（新規 mount オプションの追加のみ。既定値で従来の挙動を保つ）
Human Spec Gate: skipped（RC3・RC14 の形は親 DD-052 論点5・8 で確定済み。本DDは実装のみ）
Codex: high
Manual Gate: なし（unit・E2E で判定できる）
External Review: なし
Evidence Level: standard
```

## 目的

ReadyCrew RC3（セル単位の読み取り専用・mount 後に変更可）・RC14（行の追加・削除だけを無効にする設定）を実装する。

## 背景・課題

- RC3 現状: 列単位（`packages/grid/src/readonly-policy.ts:79-92` `partitionReadOnlyColumnChanges`/`touchesReadOnlyColumn`）・行単位（94-114行目 同型）のみ。共通ヘルパ `partitionBy`（57-71行目）で対称に実装されている。セル単位（row×col）の判定関数は無い
- RC14 現状: 単独モードで Ctrl+Shift++／Ctrl+- が行を増減する（`mount-controller.ts:2997-3026`）。判定は純関数 `decideRowStructureKey`（`row-operations.ts:32`）に委譲されており、`isComposing`/`phase`/`altKey` は見るが readOnly 系の判定は無い。止める手段は現状 `readOnly`（全編集停止）のみ
- 親 DD-052 決定:
  - 論点5 (a): 単独グリッドの行データ `GridStandaloneRow` に `readOnlyColumns?: string[]` を持たせ、`setData` で更新する（行データと同じ経路で変わり remount しない）。共同編集モードは対象外（権限は将来スコープ）
  - 論点8 (RC14 提示のとおり): `rowOperations?: boolean`（既定 `true`）

## 検討内容

- RC3 の `GridStandaloneRow.readOnlyColumns` は既存の mount オプション `GridCommonMountOptions.readOnlyColumns`（列全体を読み取り専用にする既存オプション）と**同名だが別スコープ**（前者は行データの一部＝その行だけの追加読み取り専用列、後者はグリッド全体の既定）。あるセル (row, col) が読み取り専用かどうかは「列全体オプションに col が含まれる」OR「その行の `readOnlyColumns` に col が含まれる」の論理和とする。Phase 1 で命名の混同リスクを再点検し、必要なら JSDoc で明確に書き分ける（フィールド名自体は親DDの決定どおり変更しない）
- RC14 は `decideRowStructureKey` の入力に `rowOperationsEnabled: boolean` を追加し、`false` なら常に `'none'` を返す形にする

## 決定事項

- `GridStandaloneRow` に `readOnlyColumns?: readonly string[]` を追加。セル (row, col) は「グリッド全体の `readOnlyColumns` に col が含まれる」または「その行の `readOnlyColumns` に col が含まれる」場合に読み取り専用
- `GridCommonMountOptions` に `rowOperations?: boolean`（既定 `true`）を追加。`false` の場合、単独モードの Ctrl+Shift++／Ctrl+- による行の追加・削除を無効化する（他の編集は従来どおり可能）

## 受け入れ基準

| # | 基準（操作 → 期待結果） | 検証方法 |
|---|------------------------|---------|
| 1 | 行データの `readOnlyColumns` に列Aを含めてその行の列Aへ入力 → 拒否される（他の行・他の列は編集可能） | unit（`readonly-policy.test.ts`） |
| 2 | `setData` で行ごとの `readOnlyColumns` を更新 → remount せず即座に反映される | unit |
| 3 | グリッド全体 `readOnlyColumns` と行の `readOnlyColumns` を併用 → 両方の条件で読み取り専用になる（論理和） | unit |
| 4 | `rowOperations: false` を指定 → Ctrl+Shift++／Ctrl+- が行を増減しない（他のキー操作は従来どおり） | unit（`row-operations.test.ts`）＋E2E |
| 5 | `rowOperations` 未指定（既定）→ 従来どおり行の追加・削除ができる | unit（既存ケースの回帰） |
| 6 | 共同編集モードでは RC3 のセル単位読み取り専用は対象外（将来スコープのまま） | unit（型・契約の確認） |
| 7 | 公開 .d.ts snapshot の差分が追加のみ。CHANGELOG（Added）・quick-start・features.json 更新 | contract test＋目視 |
| 8 | 既存 unit・invariants・E2E が無修正で green | 全回帰 |

## タスク一覧

### Phase 1: 実読・詳細化
- [x] 📐 実読: `readonly-policy.ts`（列版・行版の共通 `partitionBy`）・`row-operations.ts`（`decideRowStructureKey`）・`mount-controller.ts`（`hasReadOnlyCells`/`isActiveCellReadOnly`/`filterReadOnlyCells`/`submitSetCells`/`submitToBackend` の二重防衛構造）・`standalone-session.ts`（`buildDocument`/`setData`）を実読。命名は親DD決定どおり `readOnlyColumns`（行データ側）を維持し、論理和はセル単位専用の第3の判定関数（`partitionReadOnlyCellChanges`/`isReadOnlyCellId`）を列版・行版と並列に追加する形で実装（読み取り専用の判定自体は列・行・セルの3種の論理和のまま整理）
- [x] 🔬 機械検証: `bash scripts/doc-check.sh` → OK

### Phase 2: 実装（Red → Green）
- [x] **Red→Green**: `readonly-columns.test.ts` に `partitionReadOnlyCellChanges`/`touchesReadOnlyCell`（行×列の組み合わせ判定）、`standalone-session.test.ts` に `isCellReadOnly`/`hasAnyCellReadOnly`（setDataでの再構築含む）、`row-operations.test.ts` に `rowOperationsEnabled:false` のケースを追加
- [x] **Green**: `GridStandaloneRow.readOnlyColumns` → `StandaloneSession` が行単位 Map で保持 → `mount-controller.ts` の `isReadOnlyCellId`/`isReadOnlyCellIndex`/`hasReadOnlyCellRows` を追加し、既存の `hasReadOnlyCells()`/`isActiveCellReadOnly()`（共有関数）を拡張することで入口ガード・textarea ロック・chokepoint（`submitSetCells`/`submitToBackend`）・範囲フィルタ（`filterReadOnlyCells`）の**全箇所に自動波及**（個別の呼び出し元コードは無改修）。`rowOperations` mount オプション → `decideRowStructureKey` の `rowOperationsEnabled`
- [x] 🔬 機械検証: `npx vitest run packages/grid` → 461 tests green

### Phase 3: 統合・ドキュメント
- [x] playground E2E 新設 `cell-readonly-row-operations.spec.ts`（RC3: 行データの readOnlyColumns・setData解除／RC14: `?rowops=0`・公開API対象外の確認）。CHANGELOG（Added）・quick-start「行単位の読み取り専用セル・行操作の無効化」新設・features.json（`column-types`・`row-ops`）更新
- [x] 🔬 機械検証: `npx playwright test apps/playground/e2e/cell-readonly-row-operations.spec.ts` → 3 tests green／contract test（facade-surface snapshot 差分=`GridStandaloneRow.readOnlyColumns`・`rowOperations` 追加のみ）／features smoke green

### 完了前チェック
- [x] 受け入れ基準を1項目ずつ照合（1〜8 すべて充足）
- [x] 😈 セルフレビュー1巡（所見はログへ）
- [x] 🔬 全回帰1回（`npm test`／`npm run typecheck`／`npm run typecheck:consumer-strict`／`npm run lint`）→ 全パス。`npm run test:e2e` は DD-052 全体完了後にまとめて実施予定

## ログ

### 2026-09-12
- DD作成。親 DD-052 の子DD分割（決定事項）に基づき起票
- 実装完了（Phase 1〜3）。セルフレビュー所見: `hasReadOnlyCellRows()`（RC3 の early-return 判定）は列/行版と違い mount 時の静的 Set ではなく `standalone.hasAnyCellReadOnly()` を都度呼ぶ（setData で変わりうるため）。呼び出し頻度は activeCell 変更・SetCells submit 時のみでレンダーループ外のため性能影響なし。alpha 配布は親 DD-052 で 5 子DD＋DD-053 をまとめて実施する（本DD単体では配布しない）

### 2026-09-13
- 親 DD-052 の Codex xhigh 実装レビュー（詳細は親DDログ参照）で本DD該当の指摘2件を採用・修正:
  - [P1] `syncCellLock()` が `hasReadOnlyCells()===false`（readOnly 指定を setData/setRows で全解除した直後）で早期 return し、以後どのセルへ移動しても textarea の readOnly ロックが解除されず固着するバグ。ガードを外し毎回 `setInputLock` を呼ぶよう修正（同値なら無操作なので無コスト）
  - [P2] scroller の dblclick ハンドラが列/行版の readOnly（`isReadOnlyRowIndex`/`isReadOnlyColumnIndex`）のみ判定し、RC3 のセル単位 readOnly（`isReadOnlyCellIndex`）を見ていなかったため、対象セルを dblclick すると（値は確定時ガードで守られるものの）編集textareaへ既存値が読み込まれてしまっていた。同じ入口へ判定を追加
  - 両件とも `apps/playground/e2e/cell-readonly-row-operations.spec.ts` へ回帰E2Eを追加し、修正前コードで実際に失敗することを確認済み
- ユーザー指示により 0.1.0-alpha.8 で DD-052 一式とまとめて配布。完了処理として本DDをアーカイブする